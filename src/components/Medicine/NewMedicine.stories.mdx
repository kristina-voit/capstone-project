import { Meta, Story } from "@storybook/addon-docs";
import NewMedicine from "./NewMedicine";

<Meta title="Components/Cards" component={NewMedicine} />

# Card component

The Card component represents a card with detailed information about a book with its title and optionally its author.

export const Template = (args) => <NewMedicine {...args} />;

<Story
  name="New Medicine"
  args={{
    color: "#",
    text: "",
    title: "",
    isActive: true,
  }}
>
  {Template.bind({})}
</Story>
